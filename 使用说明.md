# 真题阅读打卡 · 使用说明

一个完全本地的桌面小工具，用来记录你每天阅读的六级真题文章（配套懒笔记题库 593 篇）。
无需安装、无需联网，数据只存在你自己电脑上。

## 怎么打开

双击 **`打开待办助手.bat`**，会弹出一个独立窗口（看起来就像普通桌面软件）。

> 想更方便？右键 `打开待办助手.bat` → 发送到 → 桌面快捷方式，以后双击桌面图标即可。
> 直接双击 `index.html` 用浏览器打开也能用，功能完全一样。

## 每天怎么用

1. 在懒笔记里读完一篇真题文章
2. 回到打卡软件，点「＋ 记录一篇」
3. 填一下信息（大部分是选择，**考试年月、题型等会记住你上次的选择**，连续录入很快）：
   - 考试年月 / 第几套 / Part（如 Part III Section A）
   - 题型、难度（B2/C1）、词数
   - 标题来源和心得备注（选填）
4. 点「保存并再来一篇」可以连续记录当天读的多篇

## 批量导入：从懒笔记直接复制

懒笔记的文章列表支持框选复制。把复制到的内容（一整块多行文本，每条记录含
考试年月·套数 / Part / 题型 / 难度 / 词数 / 精读 几行）直接粘贴进打卡软件：

1. 点首页的 **「📋 批量粘贴导入」**
2. 粘贴内容（开头的序号行、"原文精读/精读" 会自动忽略）
3. 如需给这批记录统一标注体裁（比如你正浏览在「议论文」分类页），在上方选一下
4. 点 **「解析预览」**，确认解析出的记录列表（已打卡过的会标黄提醒）
5. 选好导入日期（默认今天），点 **「✓ 导入 N 条」** 完成

## 各页面功能

| 页面 | 用途 |
|------|------|
| 今日 | 今日进度环（每日目标默认 2 篇）、录入表单、今日已读列表 |
| 历史 | 全部记录，支持按关键词搜索（含套数）、按题型/难度/考试年月筛选、编辑、删除 |
| 日历 | 打卡月历，有记录的日期带数字角标，点某天可查看当天明细 |
| 统计 | 累计篇数、词数、用时、阅读速度、14 天柱状图、**题库覆盖矩阵**、题型/体裁分布、里程碑徽章 |
| 设置 | 修改每日目标、题库总篇数（默认 593）、导出/导入备份、清空数据 |

录入表单里的**生词数 / 错题数 / 用时**是选填的质量字段——填了词数和用时后会自动算出阅读速度，统计页会汇总。表单内按 **Ctrl+Enter** 可直接保存。

**复习提醒**：精读过的文章会按 7 / 14 / 30 天间隔出现在今日页的「🔁 复习提醒」卡片里，重读一遍后点「已重读，记一篇」即可续期；读满 4 遍视为掌握、不再提醒。

**题库覆盖矩阵**在统计页：六级历年场次 × 套数的网格，颜色越深读得越多，点格子会自动跳到历史页并筛选出那场考试的记录。

**本周小结**（统计页顶部）：近 7 天的篇数 / 词数 / 用时 / 打卡天数，和上周对比；旁边的「🖼 生成打卡分享图」可以一键生成一张适合发学习群 / 朋友圈的 PNG 成就图。

**质量分析**（统计页）：按题型汇总平均用时、平均速度、平均错题、平均生词，哪类题错得多、读得慢，一目了然。

顶栏的「🔥 连续 N 天」是连续打卡天数徽章；统计页底部还有精读篇数 / 连续天数 / 累计词数的里程碑徽章。

## 线上版与手机使用

线上版地址：**https://lii-lii321.github.io/cet6-reading-log/**

- 用 Edge / Chrome 打开后，点地址栏右侧「安装」图标，即可安装为桌面应用（PWA）
- 手机浏览器打开同一地址也能安装，通勤时读完就能打卡
- 断网可用（已做离线缓存）
- 注意：线上版和本地文件版数据互相独立，切换时用「导出/导入备份」迁移

## 数据安全（重要）

- 所有数据保存在**本机浏览器的 localStorage** 中，不上传任何服务器。
- 换电脑、重装系统、或清理浏览器数据**会导致丢失**，请养成习惯：
  定期到「设置」页点 **⬇ 导出备份**，保存一个 json 文件。
- 超过 7 天没有备份时，首页会自动出现提醒横幅（可点「本周不再提醒」暂时关闭）。
- 换电脑或恢复数据时，用「⬆ 导入备份」选择该 json 文件即可（支持合并去重）。

## 文件清单

- `index.html` —— 软件本体（单文件，可随意复制到别处使用）
- `打开待办助手.bat` —— 以独立窗口启动
- `使用说明.md` —— 本文件
- `README.md` / `LICENSE` / `docs/` —— GitHub 仓库用说明文件与截图
